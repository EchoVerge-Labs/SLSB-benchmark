# Curated for the SLSB benchmark package (see pyproject.toml for the
# authoritative dependency list -- `pip install -e .` is the supported path).
#
# torch/torchaudio: install as a matched pair from the platform-appropriate
# source. On the ARM64 DGX Spark (GB10, aarch64), use scripts in
# data_prep or the upstream Basemodel_Benchmark/scripts/setup_spark.sh
# pattern -- native aarch64 CUDA wheels from PyPI, NOT flash-attn (unsupported
# on this hardware).
torch
torchaudio
transformers

# s3prl is in maintenance mode but remains the de facto standard for
# SUPERB-style benchmarking; pinned rather than left floating.
s3prl==0.4.17

datasets
huggingface_hub
soundfile
librosa
scipy
numpy
scikit-learn
jiwer
mlflow
dagshub
omegaconf
pydantic
rich
PyYAML

# Data versioning (data/ is DVC-tracked, remote is DagsHub)
dvc
dvc-s3
